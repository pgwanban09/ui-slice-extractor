# 调用示例

$ui-slice-extractor

我只给你一张 png，没有图层结构。
请你像做 UI 切图一样，靠视觉理解深度扫描整张图，提取所有前端可复用静态资源。

要求：
1. 不要只看我框起来的区域，要扫全图
2. 找出 logo、icon、背景图、组件壳、装饰元素、小状态图
3. 能 SVG 重建的用 SVG
4. SVG 复刻不自然的，比如柔光相机、插画装饰、食物背景，可以走生图重建
5. 生图后要透明化，透明 PNG 必须是真透明
6. 背景图不要带业务文字和内容卡片
7. 输出 preview、manifest、validation-report 和 zip
